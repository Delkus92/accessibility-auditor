# Backend — copy to .env and fill in values
PORT=3001
NODE_ENV=production

# Your deployed frontend URL (set this after deploying frontend)
FRONTEND_URL=https://your-frontend.onrender.com

# Anthropic API key — required for report generation (optional for scanning)
ANTHROPIC_API_KEY=sk-ant-...
